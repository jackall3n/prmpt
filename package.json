{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples"
  ],
  "scripts": {
    "test": "bun test",
    "lint": "bunx --bun tsc --noEmit",
    "version": "bumpp --recursive"
  },
  "devDependencies": {
    "bumpp": "^10.4.1",
    "@biomejs/biome": "^2.4.4",
    "@types/bun": "latest",
    "prmpt": "workspace:*",
    "typescript": "^5"
  }
}
