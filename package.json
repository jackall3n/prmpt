{
  "name": "tsprompt-monorepo",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "packages/*",
    "examples"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev --parallel",
    "release": "turbo run release",
    "version": "bumpp --recursive"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@types/bun": "latest",
    "bumpp": "^10.4.1",
    "@tsprompt/core": "workspace:*",
    "turbo": "^2.8.11",
    "typescript": "^5"
  }
}
