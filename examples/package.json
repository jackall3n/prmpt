{
  "name": "@prmpt/example",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "prmpt build",
    "dev": "prmpt dev . --port 3001",
    "generate": "prmpt build && bun run use.ts"
  },
  "dependencies": {
    "prmpt": "workspace:*",
    "handlebars": "^4"
  }
}
