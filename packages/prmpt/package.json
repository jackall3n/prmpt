{
  "name": "prmpt",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "prmpt": "./src/cli.ts"
  },
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "bun build.ts",
    "test": "bun test",
    "lint": "tsc --noEmit",
    "dev": "echo 'CLI package - no dev server'",
    "release": "bun publish --allow-empty"
  },
  "dependencies": {
    "@prmpt/core": "workspace:*",
    "@prmpt/dev": "workspace:*"
  }
}
