{
  "name": "@tsprompt/example",
  "private": true,
  "version": "0.0.6",
  "type": "module",
  "scripts": {
    "build": "prmpt build",
    "dev": "prmpt dev . --port 3001",
    "generate": "prmpt build && bun run use.ts"
  },
  "dependencies": {
    "@tsprompt/cli": "workspace:*",
    "handlebars": "^4"
  }
}
